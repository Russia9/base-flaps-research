FoamFile { version 2.0; format ascii; class volScalarField; object k; }
#include "../constant/freestreamProperties"

dimensions      [0 2 -2 0 0 0 0];

internalField   uniform $kInf;

boundaryField
{
    inlet       { type fixedValue; value uniform $kInf; }
    outlet      { type zeroGradient; }
    farfield    { type freestream; freestreamValue uniform $kInf; }
    fuselage    { type kqRWallFunction; value uniform $kInf; }
    stabilizers { type kqRWallFunction; value uniform $kInf; }
}
